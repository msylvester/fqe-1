class User:

	def __init__(self, alerts, first_name, last_name, gender, is_admin, locale, profile_pic, timezone):
		self.alerts = alerts
		self.first_name = first_name
		self.last_name = last_name
		self.gender = gender
		self.is_admin = is_admin
		self.locale = locale
		self.profile_pic = profile_pic
		self.timezone = timezone